{% extends 'security/login.html.twig' %}

{% block title %}Reset your password
{% endblock %}
{% block bodyclass %}class="login"{% endblock %}
{% block body %}
	{% for flashError in app.flashes('reset_password_error') %}
		<div class="alert alert-danger" role="alert">{{ flashError }}</div>
	{% endfor %}
	<div class="login_wrapper">
		<div class="form login_form">
			<section class="login_content">
				<h1>Renitialiser le mot de passe</h1>
				{{ form_start(requestForm) }}
				{{ form_row(requestForm.email) }}
				<div>
					<small>
						Entrer votre adresse address et nous vous enverons le
												                lien pour renitialiser votre mot de passe
					</small>
				</div>

				<button class="btn btn-primary">Envoyer l'email de renitialisation</button>
				{{ form_end(requestForm) }}
			</section>
		</div>
	</div>
{% endblock %}
