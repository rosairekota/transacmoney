<div class="col-md-12 mb-4">
    <div class="row">
        <div class="col-xl-4 col-lg-6">
            <div class="card card-stats mb-4 mb-xl-0">
                <div class="card-body">
                    <div class="row">
                        <div class="col">
                            <h6 class="card-title text-uppercase text-muted mb-0">
                                montant minimum a envoyer</h6>
                            <span class="h2 font-weight-bold mb-0">{{ setting.minAmount
                                }}</span>
                        </div>

                    </div>
                    <p class="mt-3 mb-0 text-muted text-sm">
                        <a href="{{ path('settings_edit', {'id': setting.id}) }}" class="btn btn-danger">Modifier</a>
                    </p>
                </div>
            </div>
        </div>
        <div class="col-xl-4 col-lg-6">
            <div class="card card-stats mb-4 mb-xl-0">
                <div class="card-body">
                    <div class="row">
                        <div class="col">
                            <h6 class="card-title text-uppercase text-muted mb-0">
                                frais d'envoi</h6>
                            <span class="h2 font-weight-bold mb-0">{{ setting.sendCommission }}</span>
                        </div>

                    </div>
                    <p class="mt-3 mb-0 text-muted text-sm">
                        <a href="{{ path('settings_edit', {'id': setting.id}) }}" class="btn btn-danger">Modifier</a>
                    </p>
                </div>
            </div>
        </div>
        <div class="col-xl-4 col-lg-6">
            <div class="card card-stats mb-4 mb-xl-0">
                <div class="card-body">
                    <div class="row">
                        <div class="col">
                            <h6 class="card-title text-uppercase text-muted mb-0">
                                Commission Agence Centrale</h6>
                            <span class="h2 font-weight-bold mb-0">{{ setting.sendCommission }}</span>
                        </div>

                    </div>
                    <p class="mt-3 mb-0 text-muted text-sm">
                        <a href="{{ path('settings_edit', {'id': setting.id}) }}" class="btn btn-danger">Modifier</a>
                    </p>
                </div>
            </div>
        </div>
    </div>
</div>
<div class="col-sm-12">
    <div class="row">
        <div class="col-xl-4 col-lg-6">
            <div class="card card-stats mb-4 mb-xl-0">
                <div class="card-body">
                    <div class="row">
                        <div class="col">
                            <h6 class="card-title text-uppercase text-muted mb-0">
                                Commission Agence Envoyeur</h6>
                            <span class="h2 font-weight-bold mb-0">{{ setting.sendProfit }}</span>
                        </div>

                    </div>
                    <p class="mt-3 mb-0 text-muted text-sm">
                        <a href="{{ path('settings_edit', {'id': setting.id}) }}" class="btn btn-danger">Modifier</a>
                    </p>
                </div>
            </div>
        </div>
        <div class="col-xl-4 col-lg-6">
            <div class="card card-stats mb-4 mb-xl-0">
                <div class="card-body">
                    <div class="row">
                        <div class="col">
                            <h6 class="card-title text-uppercase text-muted mb-0">
                                Commission sur Retrait</h6>
                            <span class="h2 font-weight-bold mb-0">{{ setting.retreatProfit }}</span>
                        </div>

                    </div>
                    <p class="mt-3 mb-0 text-muted text-sm">
                        <a href="{{ path('settings_edit', {'id': setting.id}) }}" class="btn btn-danger">Modifier</a>
                    </p>
                </div>
            </div>
        </div>
    </div>
</div>