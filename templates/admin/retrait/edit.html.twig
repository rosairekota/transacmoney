{% extends 'base.html.twig' %}

{% block title %}Edit Retrait{% endblock %}

{% block body %}
    <h1>Edit Retrait</h1>

    {{ include('retrait/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('retrait_index') }}">back to list</a>

    {{ include('retrait/_delete_form.html.twig') }}
{% endblock %}
