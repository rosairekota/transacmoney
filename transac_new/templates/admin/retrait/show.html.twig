{% extends 'base.html.twig' %}

{% block title %}Depot{% endblock %}

{% block body %}
    <h1>Depot</h1>

    <table class="table">
        <tbody>
           
            <tr>
                <th>Montant</th>
                <td>{{ retrait.depot.montant }}</td>
            </tr>
            <tr>
                <th>CodeDepot</th>
                <td>{{ retrait.depot.codeDepot }}</td>
            </tr>
            
            <tr>
                <th>MontantCommission</th>
                <td>{{ retrait.depot.montantCommission }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('depot_index') }}" class="btn btn-success">back to list</a>

    <a href="{{ path('retrait_edit', {'id': retrait.id}) }}" class="btn btn-prmary btn-md">edit</a>

    {{ include('admin/retrait/_delete_form.html.twig') }}
{% endblock %}
